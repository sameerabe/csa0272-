#include<stdio.h>
int main()
{
  int Number, i, Sum = 0;
  
  printf(" Enter any Integer Value");
  scanf("%d", &Number);
  
  for(i = 1; i <= Number; i++)
  {
     Sum = Sum + i;
  }
  
  printf("Sum  = %d", Sum);
  return 0;
}
